server:
  port: 8088

spring:
  application:
    name: story-service
  datasource:
    url: jdbc:postgresql://localhost:5432/fyn_stories
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
